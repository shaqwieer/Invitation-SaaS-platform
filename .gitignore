node_modules/
dist/
build/
.next/
out/
coverage/

# Incremental-compile caches — machine-specific, and noisy in every diff.
*.tsbuildinfo

.env
.env.*
!.env.example

# Machine-local compose overrides. Kept out of git so a fresh clone — and the
# VPS deploy in particular — gets the documented stack, not someone's local
# port remaps.
docker-compose.override.yml

*.log
npm-debug.log*
.DS_Store
Thumbs.db

.vscode/
.idea/

# Prisma generated client
apps/api/node_modules/.prisma/
apps/api/src/generated/

# Uploads (local dev)
apps/api/uploads/
